import { StyleSheet } from "react-native";

export const Style = StyleSheet.create({
    inputText: {
      borderRadius: 15,
      width: "80%",
      borderWidth: 0.5,
      paddingLeft: 10,
    },
  });